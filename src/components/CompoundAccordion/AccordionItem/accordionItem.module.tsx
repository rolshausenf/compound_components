import React from "react";

const AccordionItem: React.FC = () => {
    // Das AccordionItem ist nur eine wrapper component um button und Inhalt zusammen zu bündeln und entsprechende Indexe
    // an die Kind components weiterzugeben. (Tipp: React.cloneElement(child, {index: index})
    return <></>
}

export default AccordionItem